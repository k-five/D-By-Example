import std.stdio;

void main( string[] args ){
	writeln( "DMD:\tThe Digital Mars D complier is the official D complier written by Walter Bright" );
	writeln( "GDC:\tA front-end of the GCC back-end, build using the open DMD complier source code" );
	writeln( "LDC:\tA compiler based on DMD front-end that uses LLVM as it complier back-end" );
}
